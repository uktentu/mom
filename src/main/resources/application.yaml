spring:
  datasource:
    url: jdbc:mysql://localhost:3306/meeting_records?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: uday.2244
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
